{% extends 'OGInversaBundle:Admin:adminbase.html.twig' %}

{% block body %}
  
<h1>ProjectItem creation</h1>

<form action="{{ path('admin_projectitem_create') }}" method="post" {{ form_enctype(form) }}>
<table class="formtable">    
{{ form_widget(form) }}
</table>
         
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_projectitem') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endblock %}